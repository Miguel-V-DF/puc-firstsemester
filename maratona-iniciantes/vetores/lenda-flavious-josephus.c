#include <stdio.h>

int main()
{
    int nC;
    scanf("%d", &nC);
    for(int i = 0; i < nC; i++) {
        int n, k;
        scanf("%d %d", &n, &k);
        int numberN[n] = {0};

    }

    return 0;
}
